# paper-select-tool-lasso-example

A [Paper.js](http://paperjs.org/) based selection tool example using the lasso tool.

Usage
-----

To run the example:

```bash
npm install
npm run build
npm start
```